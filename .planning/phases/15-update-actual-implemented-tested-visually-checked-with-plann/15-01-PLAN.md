---
phase: 15-update-actual-implemented-tested-visually-checked-with-plann
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - demo/src/registry.ts
  - .planning/STATE.md
  - .planning/ROADMAP.md
autonomous: true
requirements:
  - REQ-15-01
  - REQ-15-02
  - REQ-15-03
tags:
  - documentation-sync
  - registry-cleanup
  - audit

must_haves:
  truths:
    - "STATE.md `## Metrics` section reports actual file-system counts (250 TSX demo files, 200 unique registered IDs) — not the stale 469/74% claim"
    - "ROADMAP.md `### Summary` table reflects the corrected ground-truth totals"
    - "demo/src/registry.ts has exactly ONE `id: 'textures'` CategoryEntry and exactly ONE `id: 'css3d'` CategoryEntry (duplicates merged)"
    - "demo/src/registry.ts contains zero duplicate demo IDs (no `webgl_textures_cube`, `css3d_periodictable`, or `webgl_css3d` appearing twice)"
    - "STATE.md records the 8 Kimi-verified demo IDs as visually verified"
    - "The demo dev server still builds successfully after the registry merge (`cd demo && pnpm build` exits 0)"
  artifacts:
    - path: ".planning/STATE.md"
      provides: "Corrected coverage metrics"
      contains: "Ported (registered): 200"
    - path: ".planning/ROADMAP.md"
      provides: "Corrected summary table + Phase 15 plan entries"
      contains: "15-01-PLAN.md"
    - path: "demo/src/registry.ts"
      provides: "Deduplicated registry"
      contains: "id: 'textures'"
  key_links:
    - from: "STATE.md `## Metrics`"
      to: "actual file system"
      via: "shell-counted figures"
      pattern: "Ported \\(registered\\): 200"
    - from: "demo/src/registry.ts categories"
      to: "unique CategoryEntry blocks"
      via: "single occurrence per id"
      pattern: "id: 'textures'"
---

<objective>
Reconcile planning documents with ground truth and fix the registry duplicate-category bug.

Purpose: Phase 14 finished but STATE.md still claims `469 ported (74%)` while the file system has 250 demo TSX files (200 registered). REMAINING_WORK.md and ROADMAP.md carry the same stale numbers. The `textures` and `css3d` categories appear twice in `demo/src/registry.ts`, causing duplicate sidebar entries and three duplicate demo IDs (`webgl_textures_cube`, `css3d_periodictable`, `webgl_css3d`). This plan corrects the numbers, records the 8 Kimi-verified demos from Phase 14, and merges the duplicate category blocks. Without this fix, Plans 02 and 03 cannot register new examples cleanly.

Output: Updated `STATE.md`, `ROADMAP.md`, and `demo/src/registry.ts` (deduplicated).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REMAINING_WORK.md
@.planning/phases/15-update-actual-implemented-tested-visually-checked-with-plann/15-CONTEXT.md
@.planning/phases/15-update-actual-implemented-tested-visually-checked-with-plann/15-RESEARCH.md
@demo/src/registry.ts

<interfaces>
<!-- registry.ts data model — already established, no new types needed -->
DemoEntry { id: string; name: string; category: string; component: () => Promise<{ default: () => JSX.Element }>; thumbnail?: string }
CategoryEntry { id: string; name: string; demos: DemoEntry[] }

<!-- Ground truth from RESEARCH.md (verified 2026-06-17) -->
- Total TSX demo files in demo/src/ (excl. index/menu-frame/canvas-frame/AutoSizeCanvas/Sector/Box3ce): 250
- Unique registered demo IDs in registry.ts: 200
- Ported but NOT registered TSX files: 52
- 8 Kimi-verified passing demo IDs:
  webgl_shadowmap_vsm, webgl_clipping_intersection, webgl_geometry_colors,
  webgl_interactive_cubes_gpu, webgl_lights_spotlight, webgl_lines_dashed,
  webgl_points_waves, webgl_postprocessing_smaa

<!-- Duplicate category locations in registry.ts (verified by grep 2026-06-17) -->
- `id: 'textures'` at lines 113 and 174 (must merge: 174-block has only `webgl_textures_cube` which duplicates 116)
- `id: 'css3d'` at lines 322 and 412 (must merge: 412-block contains 3 demos, 2 duplicate of 322)
- Duplicate demo IDs to remove: `webgl_textures_cube` (line 177), `css3d_periodictable` (line 415), `webgl_css3d` (line 416)
- Demo to KEEP from 412 block (not a duplicate): `webgl_css3d_demo` at line 417 → merge into 322 block
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Audit file-system counts and record ground truth</name>
  <files>
    .planning/phases/15-update-actual-implemented-tested-visually-checked-with-plann/15-01-AUDIT.md
  </files>
  <read_first>
    - .planning/phases/15-update-actual-implemented-tested-visually-checked-with-plann/15-RESEARCH.md (ground truth numbers in `## Ground Truth: Current Coverage Numbers`)
    - .planning/phases/15-update-actual-implemented-tested-visually-checked-with-plann/15-CONTEXT.md (D-06 documentation audit requirement)
    - .planning/REMAINING_WORK.md (stale numbers to reconcile)
    - demo/src/registry.ts (full file — confirm unique demo ID count)
    - playwright.test/test-results/kimi-comparison-report-fixed.json (8 verified demo IDs)
  </read_first>
  <action>
    Generate a new audit file `.planning/phases/15-update-actual-implemented-tested-visually-checked-with-plann/15-01-AUDIT.md` containing exactly these sections with concrete shell-counted numbers:

    Section "## File System Counts (run from project root)":
      - Run command (PowerShell): `(Get-ChildItem demo/src -Filter *.tsx | Where-Object { $_.Name -notmatch '^(index|menu-frame|canvas-frame|AutoSizeCanvas|Sector|Box3ce)' }).Count`
        Record the integer output as `tsx_demo_files_count`.
      - Run command: extract unique IDs from registry.ts via PowerShell: `(Select-String -Path demo/src/registry.ts -Pattern "\{ id: '([^']+)'" -AllMatches).Matches.Groups | Where-Object {$_.Name -eq "1"} | Select-Object -ExpandProperty Value | Sort-Object -Unique | Measure-Object`
        Record the integer output as `unique_registered_ids`.

    Section "## Duplicate Detection":
      - Confirm `id: 'textures'` appears at registry.ts lines 113 and 174 (use Grep with `-n`).
      - Confirm `id: 'css3d'` appears at registry.ts lines 322 and 412.
      - Confirm duplicate demo IDs: `webgl_textures_cube` (lines 116 and 177), `css3d_periodictable` (lines 325 and 415), `webgl_css3d` (lines 326 and 416).
      - Confirm `webgl_css3d_demo` (line 417) is UNIQUE — it must be preserved when merging.

    Section "## Kimi-Verified Demos (Phase 14)":
      - List exactly 8 demo IDs (from RESEARCH.md `## Visual Verification Status`): webgl_shadowmap_vsm, webgl_clipping_intersection, webgl_geometry_colors, webgl_interactive_cubes_gpu, webgl_lights_spotlight, webgl_lines_dashed, webgl_points_waves, webgl_postprocessing_smaa.

    Section "## Delta vs STATE.md Claims":
      - Stale claim: 469 ported (74%). Actual: `unique_registered_ids` registered out of 629 (~32%). Compute and record actual percentage.

    Do NOT modify STATE.md, ROADMAP.md, or registry.ts in this task — the next tasks consume this audit. The audit file is the single source of truth.
  </action>
  <verify>
    <automated>powershell -Command "if ((Test-Path '.planning/phases/15-update-actual-implemented-tested-visually-checked-with-plann/15-01-AUDIT.md') -and (Select-String -Path '.planning/phases/15-update-actual-implemented-tested-visually-checked-with-plann/15-01-AUDIT.md' -Pattern 'tsx_demo_files_count' -Quiet) -and (Select-String -Path '.planning/phases/15-update-actual-implemented-tested-visually-checked-with-plann/15-01-AUDIT.md' -Pattern 'webgl_shadowmap_vsm' -Quiet)) { exit 0 } else { exit 1 }"</automated>
  </verify>
  <acceptance_criteria>
    1. File `.planning/phases/15-update-actual-implemented-tested-visually-checked-with-plann/15-01-AUDIT.md` exists.
    2. AUDIT.md contains a literal `tsx_demo_files_count: <integer>` line where the integer is the actual count from the PowerShell command (expected ~250).
    3. AUDIT.md contains a literal `unique_registered_ids: <integer>` line where the integer is the actual count (expected ~200).
    4. AUDIT.md lists all 8 Kimi-verified demo IDs in the "Kimi-Verified Demos" section.
    5. AUDIT.md confirms the duplicate category line numbers exactly: `id: 'textures'` at 113 and 174, `id: 'css3d'` at 322 and 412.
    6. AUDIT.md does NOT modify STATE.md, ROADMAP.md, or registry.ts (verified by `git status` showing only the AUDIT.md as new file).
  </acceptance_criteria>
  <done>15-01-AUDIT.md exists with shell-counted ground-truth numbers and Kimi-verified demo list; no other files modified.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Merge duplicate category blocks in demo/src/registry.ts</name>
  <files>
    demo/src/registry.ts
  </files>
  <read_first>
    - demo/src/registry.ts (current file — read FULLY before editing)
    - .planning/phases/15-update-actual-implemented-tested-visually-checked-with-plann/15-01-AUDIT.md (confirmed duplicate locations)
    - .planning/phases/15-update-actual-implemented-tested-visually-checked-with-plann/15-RESEARCH.md `## Known Registry Bug: Duplicate Categories` section
  </read_first>
  <action>
    Edit `demo/src/registry.ts` to merge duplicate category blocks. Make the following EXACT changes:

    Change 1 — Merge `textures` categories:
      - Inside the FIRST `textures` CategoryEntry block (starts at the line containing `id: 'textures'` near line 113), the demos array currently contains:
        `{ id: 'webgl_textures_cube', ... }` and `{ id: 'webgl_uvs', ... }`.
        Leave both entries as-is.
      - Locate the SECOND `textures` CategoryEntry block (starts at the line containing `id: 'textures'` near line 174). It contains only `{ id: 'webgl_textures_cube', ... }` — a duplicate.
      - DELETE the entire second `textures` CategoryEntry block, including the opening `{`, the `id: 'textures'`, `name: 'Textures'`, `demos: [ ... ]`, and the closing `},` (including the trailing comma).

    Change 2 — Merge `css3d` categories:
      - Inside the FIRST `css3d` CategoryEntry block (starts at the line containing `id: 'css3d'` near line 322), the demos array currently contains `{ id: 'css3d_periodictable', ... }` and `{ id: 'webgl_css3d', ... }`. Keep both.
      - Locate the SECOND `css3d` CategoryEntry block (starts at the line containing `id: 'css3d'` near line 412). It contains THREE demos: `css3d_periodictable` (duplicate), `webgl_css3d` (duplicate), and `webgl_css3d_demo` (UNIQUE — must preserve).
      - Add the `webgl_css3d_demo` entry to the FIRST `css3d` block's demos array, immediately after the `webgl_css3d` entry:
        `{ id: 'webgl_css3d_demo', name: 'CSS3D Demo', category: 'css3d', component: () => import('./WebGLCSS3D') },`
      - DELETE the entire second `css3d` CategoryEntry block (the one near line 412), including all three demo entries and the closing `},`.

    Do NOT modify any other category blocks or demo entries. Do NOT rename or reorder existing demos. Do NOT alter `import type { JSX } from 'woby'`, the `DemoEntry`/`CategoryEntry` interfaces, or the `categories` / `allDemos` exports.

    After edits, the file MUST contain:
      - Exactly ONE occurrence of `id: 'textures'`
      - Exactly ONE occurrence of `id: 'css3d'`
      - Exactly ONE occurrence of `id: 'webgl_textures_cube'`
      - Exactly ONE occurrence of `id: 'css3d_periodictable'`
      - Exactly ONE occurrence of `id: 'webgl_css3d'`
      - Exactly ONE occurrence of `id: 'webgl_css3d_demo'`
  </action>
  <verify>
    <automated>powershell -Command "$f = 'demo/src/registry.ts'; $tx = (Select-String -Path $f -Pattern \"id: 'textures'\" -AllMatches).Matches.Count; $c3 = (Select-String -Path $f -Pattern \"id: 'css3d'\" -AllMatches).Matches.Count; $tc = (Select-String -Path $f -Pattern \"id: 'webgl_textures_cube'\" -AllMatches).Matches.Count; $pt = (Select-String -Path $f -Pattern \"id: 'css3d_periodictable'\" -AllMatches).Matches.Count; $wc = (Select-String -Path $f -Pattern \"id: 'webgl_css3d'\" -AllMatches).Matches.Count; $wd = (Select-String -Path $f -Pattern \"id: 'webgl_css3d_demo'\" -AllMatches).Matches.Count; if ($tx -eq 1 -and $c3 -eq 1 -and $tc -eq 1 -and $pt -eq 1 -and $wc -eq 1 -and $wd -eq 1) { Write-Host 'PASS'; exit 0 } else { Write-Host \"FAIL textures=$tx css3d=$c3 cube=$tc periodic=$pt css3d_id=$wc css3d_demo=$wd\"; exit 1 }"</automated>
  </verify>
  <acceptance_criteria>
    1. `demo/src/registry.ts` contains exactly ONE line matching `id: 'textures',` (CategoryEntry id).
    2. `demo/src/registry.ts` contains exactly ONE line matching `id: 'css3d',` (CategoryEntry id).
    3. `demo/src/registry.ts` contains exactly ONE line matching `id: 'webgl_textures_cube'`.
    4. `demo/src/registry.ts` contains exactly ONE line matching `id: 'css3d_periodictable'`.
    5. `demo/src/registry.ts` contains exactly ONE line matching `id: 'webgl_css3d'` (NOTE: the substring also appears in `webgl_css3d_demo`, so the assertion must use the closing quote: count of `id: 'webgl_css3d',` is 1).
    6. `demo/src/registry.ts` contains exactly ONE line matching `id: 'webgl_css3d_demo'` (preserved from merged second block).
    7. `cd demo && pnpm build` exits 0 (registry compiles, no missing imports).
    8. The file's `categories` array still ends with the same closing `]` and `export const allDemos` is still present.
  </acceptance_criteria>
  <done>registry.ts has exactly one `textures` and one `css3d` CategoryEntry, three duplicate demo IDs removed, `webgl_css3d_demo` preserved, build passes.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Update STATE.md and ROADMAP.md with ground-truth numbers</name>
  <files>
    .planning/STATE.md
    .planning/ROADMAP.md
  </files>
  <read_first>
    - .planning/STATE.md (current stale claims at lines 54-59)
    - .planning/ROADMAP.md (Summary table at lines 495-514, Phase 15 entry at lines 449-460)
    - .planning/phases/15-update-actual-implemented-tested-visually-checked-with-plann/15-01-AUDIT.md (the source-of-truth integers produced by Task 1)
    - .planning/phases/15-update-actual-implemented-tested-visually-checked-with-plann/15-RESEARCH.md (`## Ground Truth: Current Coverage Numbers`)
  </read_first>
  <action>
    Edit `.planning/STATE.md`:

    1. Replace the entire `## Metrics` section (currently at lines 54-59) with:
       ```
       ## Metrics

       Source: file-system scan + registry.ts grep + kimi-comparison-report-fixed.json (verified 2026-06-17 — see .planning/phases/15-update-actual-implemented-tested-visually-checked-with-plann/15-01-AUDIT.md).

       - Total Three.js Examples (upstream): 629
       - Ported (registered in registry.ts, unique IDs): 200
       - Ported but NOT registered (TSX files in demo/src/ awaiting verification): 52
       - Total TSX demo files: 250
       - Coverage (registered / upstream): 32%
       - Visually verified via Kimi (similarity >= 0.7): 8
       - Visually failed via Kimi (similarity < 0.7): 108
       - Kimi comparison errors (could not compare): 18
       - Test suites: 8 (postprocessing, webgpu, webxr, physics, advanced, misc, css-svg, audio)
       ```
       Use the integers from `15-01-AUDIT.md` if they differ from these expected values; the audit file is the source of truth.

    2. After the `## Metrics` section, insert a new `## Visually Verified Demos (Phase 14 Kimi Verdicts)` section containing:
       ```
       Source: playwright.test/test-results/kimi-comparison-report-fixed.json

       Passed (similarity >= 0.7):
       - webgl_shadowmap_vsm
       - webgl_clipping_intersection
       - webgl_geometry_colors
       - webgl_interactive_cubes_gpu
       - webgl_lights_spotlight
       - webgl_lines_dashed
       - webgl_points_waves
       - webgl_postprocessing_smaa
       ```

    3. Update the `progress:` frontmatter block:
       - `total_phases: 15`
       - `completed_phases: 14`
       - `total_plans` should be incremented by 3 (for Plans 15-01, 15-02, 15-03)
       Set `completed_plans` unchanged.

    4. Update `last_updated:` to today's ISO date.

    5. Append a new `## Phase 15 Progress` section parallel to the existing `## Phase 14 Progress` section, with a table row per plan (all `IN PROGRESS` initially).

    Edit `.planning/ROADMAP.md`:

    6. In the `### Summary` table (lines 495-514), append a new row for Phase 15:
       `| 15 | sync | 1-2 days | IN PROGRESS |`

    7. Update the `**Total Examples**` line (currently `~574`) to `~629` (matches the upstream Three.js count used in STATE.md).

    8. Update the `**Completed**` line to `200 (registered) / 250 (TSX files)`.

    9. Update the `**Remaining**` line to `~429 upstream examples unported`.

    10. In the Phase 15 entry (lines 449-460), update the Plans list from `**Plans:** 0 plans` to `**Plans:** 3 plans` and replace the existing Plans bullet list with:
        ```
        Plans:
        - [ ] 15-01-PLAN.md — Documentation audit + registry duplicate fix
        - [ ] 15-02-PLAN.md — Port webgl examples — batch 1 (animation/clipping priority, 5 examples)
        - [ ] 15-03-PLAN.md — Port webgl examples — batch 2 (camera/interactive priority, 5 examples)
        ```

    Do NOT touch any other section. Do NOT modify completed phase entries except where explicitly listed above.
  </action>
  <verify>
    <automated>powershell -Command "$ok = $true; if (-not (Select-String -Path '.planning/STATE.md' -Pattern 'Ported \(registered in registry.ts, unique IDs\): 200' -Quiet)) { Write-Host 'STATE missing registered count'; $ok = $false }; if (-not (Select-String -Path '.planning/STATE.md' -Pattern 'webgl_shadowmap_vsm' -Quiet)) { Write-Host 'STATE missing kimi list'; $ok = $false }; if (-not (Select-String -Path '.planning/STATE.md' -Pattern 'Phase 15 Progress' -Quiet)) { Write-Host 'STATE missing Phase 15 section'; $ok = $false }; if (-not (Select-String -Path '.planning/ROADMAP.md' -Pattern '15-01-PLAN.md.*Documentation audit' -Quiet)) { Write-Host 'ROADMAP missing 15-01 plan entry'; $ok = $false }; if (-not (Select-String -Path '.planning/ROADMAP.md' -Pattern '15-02-PLAN.md' -Quiet)) { Write-Host 'ROADMAP missing 15-02 plan entry'; $ok = $false }; if (-not (Select-String -Path '.planning/ROADMAP.md' -Pattern '15-03-PLAN.md' -Quiet)) { Write-Host 'ROADMAP missing 15-03 plan entry'; $ok = $false }; if ($ok) { exit 0 } else { exit 1 }"</automated>
  </verify>
  <acceptance_criteria>
    1. `.planning/STATE.md` `## Metrics` section contains the literal string `Ported (registered in registry.ts, unique IDs): 200` (or the actual integer from 15-01-AUDIT.md if it differs).
    2. `.planning/STATE.md` contains a `## Visually Verified Demos` section listing all 8 Kimi-passing demo IDs verbatim.
    3. `.planning/STATE.md` does NOT contain the stale strings `Ported: 469 (74%` or `Coverage: 74%` (the 74% claim is removed).
    4. `.planning/STATE.md` contains a `## Phase 15 Progress` section with a row for each plan (15-01, 15-02, 15-03).
    5. `.planning/STATE.md` frontmatter has `total_phases: 15` and `last_updated:` set to today's ISO date.
    6. `.planning/ROADMAP.md` `### Summary` table contains a row for Phase 15.
    7. `.planning/ROADMAP.md` Phase 15 section lists three plans (`15-01-PLAN.md`, `15-02-PLAN.md`, `15-03-PLAN.md`) instead of `**Plans:** 0 plans`.
    8. `.planning/ROADMAP.md` `**Total Examples**` line reads `~629` (corrected from `~574`).
  </acceptance_criteria>
  <done>STATE.md reflects real coverage (200 registered, 250 TSX, 8 Kimi-verified) and lists 8 Kimi-passing demos; ROADMAP.md summary table includes Phase 15 row and Phase 15 entry lists three plans.</done>
</task>

</tasks>

<verification>
End-of-plan checks:
1. `cd demo && pnpm build` exits 0 (registry compiles without TS errors).
2. `pnpm dev` starts at http://localhost:5173 and the sidebar shows ONE `Textures` section and ONE `CSS3D` section (no duplicates).
3. STATE.md `## Metrics` numbers match the output of: `(Get-ChildItem demo/src -Filter *.tsx | Where-Object { $_.Name -notmatch '^(index|menu-frame|canvas-frame|AutoSizeCanvas|Sector|Box3ce)' }).Count` for the TSX count, and the unique-id count from registry.ts.
4. ROADMAP.md summary table includes a row for Phase 15.
5. Git status shows exactly four modified files: `demo/src/registry.ts`, `.planning/STATE.md`, `.planning/ROADMAP.md`, plus the new `15-01-AUDIT.md`.
</verification>

<success_criteria>
- Audit file (15-01-AUDIT.md) records ground-truth counts from PowerShell commands
- registry.ts has exactly one `textures` and one `css3d` CategoryEntry; zero duplicate demo IDs; build passes
- STATE.md numbers come from file-system count, not AI self-report
- 8 Kimi-verified demo IDs explicitly listed in STATE.md
- ROADMAP.md summary includes Phase 15 row and 3 plan entries
- Stale strings `469 (74%)`, `Coverage: 74%`, `~574` removed
</success_criteria>

<output>
Create `.planning/phases/15-update-actual-implemented-tested-visually-checked-with-plann/15-01-SUMMARY.md` when done — record audit numbers, registry diff stats, and STATE.md/ROADMAP.md change summary.
</output>
