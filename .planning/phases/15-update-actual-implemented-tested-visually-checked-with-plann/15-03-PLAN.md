---
phase: 15-update-actual-implemented-tested-visually-checked-with-plann
plan: 03
type: execute
wave: 2
depends_on:
  - 15-01
files_modified:
  - demo/src/WebGLCameraLogarithmicDepthBuffer2.tsx
  - demo/src/WebGLInteractiveBufferGeometry.tsx
  - demo/src/WebGLInteractiveCubesOrtho.tsx
  - demo/src/WebGLGeometryTextStroke.tsx
  - demo/src/WebGLModifierCurve.tsx
  - demo/src/registry.ts
  - playwright.test/screenshots/ported/webgl_camera_logarithmicdepthbuffer.png
  - playwright.test/screenshots/ref/webgl_camera_logarithmicdepthbuffer.png
  - playwright.test/screenshots/ported/webgl_interactive_buffergeometry.png
  - playwright.test/screenshots/ref/webgl_interactive_buffergeometry.png
  - playwright.test/screenshots/ported/webgl_interactive_cubes_ortho.png
  - playwright.test/screenshots/ref/webgl_interactive_cubes_ortho.png
  - playwright.test/screenshots/ported/webgl_geometry_text_stroke.png
  - playwright.test/screenshots/ref/webgl_geometry_text_stroke.png
  - playwright.test/screenshots/ported/webgl_modifier_curve.png
  - playwright.test/screenshots/ref/webgl_modifier_curve.png
autonomous: false
requirements:
  - REQ-15-04
tags:
  - porting
  - webgl
  - camera
  - interactive
  - visual-verification

user_setup:
  - service: dev-server
    why: "Must be running for visual verification — manual one-time start (same instance as Plan 02)"
    setup_steps:
      - "Confirm Vite dev server from Plan 02 is still running at http://localhost:5173"
      - "If not, in a dedicated terminal: cd demo && pnpm dev"
      - "Keep this terminal open for the duration of the plan"

must_haves:
  truths:
    - "Each ported example renders a non-blank WebGL canvas at http://localhost:5173/#<id>"
    - "Each ported example was visually compared against threejs.org/examples/<id>.html and Claude APPROVED it in the conversation"
    - "Each ported example uses the init3D container-ref pattern (no JSX scene-graph wrappers)"
    - "Each ported example file starts with `/** @jsxImportSource woby */`"
    - "Each ported example cleans up rAF, listeners, GUI, controls, renderer in _cleanupFn"
    - "Each ported example is registered in demo/src/registry.ts under the correct category exactly once"
    - "demo/ builds successfully after all entries are added (`pnpm build` exits 0)"
  artifacts:
    - path: "demo/src/WebGLCameraLogarithmicDepthBuffer2.tsx"
      provides: "Port of webgl_camera_logarithmicdepthbuffer"
      contains: "/** @jsxImportSource woby */"
      min_lines: 40
    - path: "demo/src/WebGLInteractiveBufferGeometry.tsx"
      provides: "Port of webgl_interactive_buffergeometry"
      contains: "init3D"
      min_lines: 40
    - path: "demo/src/WebGLInteractiveCubesOrtho.tsx"
      provides: "Port of webgl_interactive_cubes_ortho"
      contains: "init3D"
      min_lines: 40
    - path: "demo/src/WebGLGeometryTextStroke.tsx"
      provides: "Port of webgl_geometry_text_stroke"
      contains: "init3D"
      min_lines: 40
    - path: "demo/src/WebGLModifierCurve.tsx"
      provides: "Port of webgl_modifier_curve"
      contains: "init3D"
      min_lines: 40
  key_links:
    - from: "demo/src/registry.ts cameras block"
      to: "WebGLCameraLogarithmicDepthBuffer2 component"
      via: "lazy import"
      pattern: "import\\('./WebGLCameraLogarithmicDepthBuffer2'\\)"
    - from: "demo/src/registry.ts interactive block"
      to: "WebGLInteractiveBufferGeometry component"
      via: "lazy import"
      pattern: "import\\('./WebGLInteractiveBufferGeometry'\\)"
    - from: "demo/src/registry.ts interactive block"
      to: "WebGLInteractiveCubesOrtho component"
      via: "lazy import"
      pattern: "import\\('./WebGLInteractiveCubesOrtho'\\)"
    - from: "demo/src/registry.ts geometries block"
      to: "WebGLGeometryTextStroke component"
      via: "lazy import"
      pattern: "import\\('./WebGLGeometryTextStroke'\\)"
---

<objective>
Port 5 Three.js webgl examples (batch 2: camera-logarithmic, interactive buffergeometry, interactive cubes ortho, text stroke geometry, modifier curve) to Woby using the init3D pattern, visually verify each against the threejs.org reference with Claude approval, then register each in `demo/src/registry.ts`.

Purpose: Adds 5 more from the REMAINING_WORK.md webgl priority list per D-02 1:1 fidelity rule and D-03 visual verification workflow. Runs in parallel with Plan 02 (Wave 2) — touches different TSX files and different categories where possible. The shared file `demo/src/registry.ts` is appended to by both plans, but each plan adds to a different CategoryEntry block (Plan 02: animation/clipping/geometries/lights; Plan 03: cameras/interactive/geometries/modifiers).

Output: 5 new TSX files, 10 PNG screenshots (5 ported + 5 reference), 5 new entries in `demo/src/registry.ts`.

NOTE on registry.ts contention: Plan 02 and Plan 03 share `demo/src/registry.ts`. Both add at the END of their respective CategoryEntry `demos` arrays. Plan 02 touches `animation` (twice), `clipping`, `geometries` (Spline Editor), and `lights`. Plan 03 touches `cameras`, `interactive`, `geometries` (Text Stroke), and creates a new `modifiers` category if absent. The only overlapping category is `geometries` — both plans append a new entry there. To avoid edit conflicts during parallel execution, **Plan 03 Task 4 must read-then-write registry.ts as a final atomic step AFTER Plan 02 has registered its geometries entry, OR the orchestrator must serialize the registry edits**. If the orchestrator runs Plan 02 and Plan 03 concurrently, the executor of the second-finishing plan MUST re-read registry.ts before its final write to incorporate the other plan's changes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/15-update-actual-implemented-tested-visually-checked-with-plann/15-CONTEXT.md
@.planning/phases/15-update-actual-implemented-tested-visually-checked-with-plann/15-RESEARCH.md
@.planning/phases/15-update-actual-implemented-tested-visually-checked-with-plann/15-PATTERNS.md
@demo/src/WebGLCamera.tsx
@demo/src/WebGLAnimationSkinningMorph.tsx
@demo/src/WebGLLightsSpotlight.tsx
@demo/src/WebGLClippingIntersection.tsx
@demo/src/registry.ts

<interfaces>
<!-- Same pattern contracts as Plan 02 — see PATTERNS.md and WebGLCamera.tsx -->

Canonical init3D module scaffold (abbreviated):
  /** @jsxImportSource woby */
  import * as THREE from 'three'
  let _cleanupFn: (() => void) | null = null
  const init3D = (container: HTMLElement) => {
    if (_cleanupFn) { _cleanupFn(); _cleanupFn = null }
    // ... setup ...
    container.appendChild(renderer.domElement)
    // ... animate ...
    _cleanupFn = () => { /* full teardown */ }
  }
  export const <Name> = () => <div style="width:100%;height:100%;position:relative"><div ref={(el: HTMLElement | null) => { if (el) init3D(el) }} style="width:100%;height:100%" /></div>
  export default <Name>

DemoEntry registry shape:
  { id: string; name: string; category: string; component: () => Promise<{ default: () => JSX.Element }>; thumbnail?: string }

Raycasting pattern (for interactive demos — VERIFIED from existing demos):
  const raycaster = new THREE.Raycaster()
  const pointer = new THREE.Vector2()
  const onPointerMove = (event: PointerEvent) => {
    const rect = renderer.domElement.getBoundingClientRect()
    pointer.x = ((event.clientX - rect.left) / rect.width) * 2 - 1
    pointer.y = -((event.clientY - rect.top) / rect.height) * 2 + 1
  }
  // in render: raycaster.setFromCamera(pointer, camera); const hits = raycaster.intersectObjects(scene.children)

Text geometry (Font + TextGeometry):
  import { FontLoader } from 'three/examples/jsm/loaders/FontLoader.js'
  import { TextGeometry } from 'three/examples/jsm/geometries/TextGeometry.js'
  const loader = new FontLoader()
  loader.load('fonts/helvetiker_regular.typeface.json', (font) => {
    const geometry = new TextGeometry('Hello', { font, size: 80, depth: 20, ... })
  })
  Font assets live in demo/public/fonts/ (verify available file names before porting).
</interfaces>

<dv_cli_reference>
<!-- Same as Plan 02 -->
Profile: profile-qmdj-2  Port: 9223

Reuse same Chrome instance if Plan 02 already started it. Otherwise:
  dv start --port 9223 --profile profile-qmdj-2 --headed

Standard sequence per demo:
  dv navigate --url "http://localhost:5173/#<demo_id>" --port 9223
  (wait 3-5s)
  dv eval --js "!!document.querySelector('canvas')" --port 9223
  dv screenshot --output "playwright.test/screenshots/ported/<demo_id>.png" --port 9223
  dv navigate --url "https://threejs.org/examples/<demo_id>.html" --port 9223
  (wait 3-5s)
  dv screenshot --output "playwright.test/screenshots/ref/<demo_id>.png" --port 9223

dv stop --port 9223 at end of Task 5 (or skip if Plan 02 will also stop it).
</dv_cli_reference>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 0: Confirm dev server is running</name>
  <what-built>This plan requires the demo dev server running at http://localhost:5173 throughout. Plan 02 may already have started it; this task confirms it is up.</what-built>
  <how-to-verify>
    1. In a browser, visit http://localhost:5173 and confirm the sidebar loads with the demo list (no blank page or 5173 connection error).
    2. If the server is NOT running, open a terminal and run: `cd demo && pnpm dev`. Wait for the `Local: http://localhost:5173/` line.
    3. Leave the terminal open for the duration of this plan.
  </how-to-verify>
  <resume-signal>Type "dev-server-up" to proceed, or describe any startup errors.</resume-signal>
</task>

<task type="auto" tdd="false">
  <name>Task 1: Port webgl_camera_logarithmicdepthbuffer</name>
  <files>
    demo/src/WebGLCameraLogarithmicDepthBuffer2.tsx
    playwright.test/screenshots/ported/webgl_camera_logarithmicdepthbuffer.png
    playwright.test/screenshots/ref/webgl_camera_logarithmicdepthbuffer.png
  </files>
  <read_first>
    - demo/src/WebGLCamera.tsx (closest analog — dual viewport split with two cameras)
    - .planning/phases/15-update-actual-implemented-tested-visually-checked-with-plann/15-PATTERNS.md
    - .planning/phases/15-update-actual-implemented-tested-visually-checked-with-plann/15-RESEARCH.md (dv CLI section)
    - demo/src/registry.ts (check `cameras` category for collisions; note Tier 1 has `WebGLCameraLogarithmicDepthBuffer.tsx` unregistered — use suffix `2` for the new canonical port to avoid file/id collision)
  </read_first>
  <action>
    1. Fetch `https://threejs.org/examples/webgl_camera_logarithmicdepthbuffer.html`. This demo splits the canvas into two viewports: left uses standard depth buffer, right uses logarithmic. It renders large-scale geometry (planets / objects at vastly different distances) showing z-fighting differences. Reproduce 1:1: two PerspectiveCamera instances, two WebGLRenderer instances (one with `logarithmicDepthBuffer: true`), split-viewport via scissor/viewport calls OR two canvas elements, scene with widely-spaced spheres/text labels, render loop animating both views.

    2. Check `demo/src/` for any existing `WebGLCameraLogarithmicDepthBuffer.tsx` (a Tier 1 unregistered file may exist per RESEARCH.md). Name the NEW canonical port `WebGLCameraLogarithmicDepthBuffer2.tsx` to avoid file collision regardless of whether the older file exists.

    3. Create `demo/src/WebGLCameraLogarithmicDepthBuffer2.tsx` using init3D pattern. The dual-renderer setup means TWO `container.appendChild()` calls and TWO renderer.dispose() calls in cleanup. The wrapper div pattern stays the same but the inner container will host two canvas elements side by side — use CSS `display:flex` or two child divs to host each renderer.

    4. Type-check: `cd demo && pnpm tsc --noEmit`.

    5. dv visual verification:
       - `dv navigate --url "http://localhost:5173/#webgl_camera_logarithmicdepthbuffer" --port 9223`
       - Wait 4 seconds (text labels need font load)
       - `dv eval --js "!!document.querySelector('canvas')" --port 9223` returns `true`
       - `dv screenshot --output "playwright.test/screenshots/ported/webgl_camera_logarithmicdepthbuffer.png" --port 9223`
       - `dv navigate --url "https://threejs.org/examples/webgl_camera_logarithmicdepthbuffer.html" --port 9223`
       - Wait 4 seconds
       - `dv screenshot --output "playwright.test/screenshots/ref/webgl_camera_logarithmicdepthbuffer.png" --port 9223`

    6. Send to Claude: `Visual verify: webgl_camera_logarithmicdepthbuffer — ported (img1) vs reference (img2). Approve or reject?` Wait for APPROVE.

    7. After approval, register in the `cameras` CategoryEntry. Use a non-colliding id — search for existing `webgl_camera_logarithmicdepthbuffer` registration first. If absent, use `webgl_camera_logarithmicdepthbuffer`. If present (Tier 1 unregistered file may exist with this id), use `webgl_camera_logarithmicdepthbuffer_canonical`:
       `{ id: '<chosen_id>', name: 'Logarithmic Depth Buffer (Canonical)', category: 'cameras', component: () => import('./WebGLCameraLogarithmicDepthBuffer2') },`
  </action>
  <verify>
    <automated>powershell -Command "$ok = $true; if (-not (Test-Path 'demo/src/WebGLCameraLogarithmicDepthBuffer2.tsx')) { $ok = $false }; if (-not (Test-Path 'playwright.test/screenshots/ported/webgl_camera_logarithmicdepthbuffer.png')) { $ok = $false }; if (-not (Test-Path 'playwright.test/screenshots/ref/webgl_camera_logarithmicdepthbuffer.png')) { $ok = $false }; if (-not (Select-String -Path 'demo/src/WebGLCameraLogarithmicDepthBuffer2.tsx' -Pattern '/\*\* @jsxImportSource woby \*/' -Quiet)) { $ok = $false }; if (-not (Select-String -Path 'demo/src/WebGLCameraLogarithmicDepthBuffer2.tsx' -Pattern 'logarithmicDepthBuffer' -Quiet)) { $ok = $false }; if (Select-String -Path 'demo/src/WebGLCameraLogarithmicDepthBuffer2.tsx' -Pattern 'as any' -Quiet) { $ok = $false }; if (-not (Select-String -Path 'demo/src/registry.ts' -Pattern 'WebGLCameraLogarithmicDepthBuffer2' -Quiet)) { $ok = $false }; if ($ok) { exit 0 } else { exit 1 }"</automated>
  </verify>
  <acceptance_criteria>
    1. `demo/src/WebGLCameraLogarithmicDepthBuffer2.tsx` exists with `/** @jsxImportSource woby */` header.
    2. File contains literal `logarithmicDepthBuffer` (renderer constructor option).
    3. File contains zero `as any` occurrences.
    4. Both screenshot files exist and are non-empty.
    5. Claude APPROVED the visual comparison in the conversation.
    6. `demo/src/registry.ts` contains an entry under `cameras` whose `component` lazy-imports `./WebGLCameraLogarithmicDepthBuffer2`.
    7. `cd demo && pnpm build` exits 0.
  </acceptance_criteria>
  <done>WebGLCameraLogarithmicDepthBuffer2.tsx ports canonical logarithmic depth demo 1:1, screenshots captured, Claude approved, registered under `cameras`.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Port webgl_interactive_buffergeometry</name>
  <files>
    demo/src/WebGLInteractiveBufferGeometry.tsx
    playwright.test/screenshots/ported/webgl_interactive_buffergeometry.png
    playwright.test/screenshots/ref/webgl_interactive_buffergeometry.png
  </files>
  <read_first>
    - demo/src/WebGLCamera.tsx (init3D scaffold)
    - demo/src/registry.ts (find `interactive` category — confirm it exists OR plan to create it)
    - .planning/phases/15-update-actual-implemented-tested-visually-checked-with-plann/15-PATTERNS.md
  </read_first>
  <action>
    1. Fetch `https://threejs.org/examples/webgl_interactive_buffergeometry.html`. This demo uses BufferGeometry with per-vertex colors, a Raycaster for mouse pick on vertices, and dynamically updates vertex colors on hover. Reproduce 1:1: BufferGeometry with positions + colors (Float32BufferAttribute), basic camera + lights, Raycaster + Vector2 pointer state, pointermove event listener, update vertex color attribute on hit.

    2. Check `demo/src/registry.ts` for the `interactive` CategoryEntry. If it does NOT exist, plan to add a new CategoryEntry: `{ id: 'interactive', name: 'Interactive', demos: [...] }`. Add this new category in alphabetical order with the surrounding categories OR at the end before `webgpu`.

    3. Create `demo/src/WebGLInteractiveBufferGeometry.tsx`:
       - jsx pragma
       - Imports: THREE, Stats (if original shows FPS)
       - init3D pattern
       - Raycaster + pointermove listener on `renderer.domElement` (NOT on `window` — keep events scoped)
       - Cleanup removes pointermove listener and disposes geometry

    4. Type-check: `cd demo && pnpm tsc --noEmit`.

    5. dv visual verification:
       - `dv navigate --url "http://localhost:5173/#webgl_interactive_buffergeometry" --port 9223`
       - Wait 3 seconds
       - `dv eval --js "!!document.querySelector('canvas')" --port 9223` returns `true`
       - `dv screenshot --output "playwright.test/screenshots/ported/webgl_interactive_buffergeometry.png" --port 9223`
       - `dv navigate --url "https://threejs.org/examples/webgl_interactive_buffergeometry.html" --port 9223`
       - Wait 3 seconds
       - `dv screenshot --output "playwright.test/screenshots/ref/webgl_interactive_buffergeometry.png" --port 9223`

    6. Send to Claude: `Visual verify: webgl_interactive_buffergeometry — ported (img1) vs reference (img2). Approve or reject?` Wait for APPROVE.

    7. After approval, register in the `interactive` CategoryEntry (creating it if needed):
       `{ id: 'webgl_interactive_buffergeometry', name: 'Interactive BufferGeometry', category: 'interactive', component: () => import('./WebGLInteractiveBufferGeometry') },`
  </action>
  <verify>
    <automated>powershell -Command "$ok = $true; if (-not (Test-Path 'demo/src/WebGLInteractiveBufferGeometry.tsx')) { $ok = $false }; if (-not (Test-Path 'playwright.test/screenshots/ported/webgl_interactive_buffergeometry.png')) { $ok = $false }; if (-not (Test-Path 'playwright.test/screenshots/ref/webgl_interactive_buffergeometry.png')) { $ok = $false }; if (-not (Select-String -Path 'demo/src/WebGLInteractiveBufferGeometry.tsx' -Pattern '/\*\* @jsxImportSource woby \*/' -Quiet)) { $ok = $false }; if (-not (Select-String -Path 'demo/src/WebGLInteractiveBufferGeometry.tsx' -Pattern 'Raycaster' -Quiet)) { $ok = $false }; if (Select-String -Path 'demo/src/WebGLInteractiveBufferGeometry.tsx' -Pattern 'as any' -Quiet) { $ok = $false }; if (-not (Select-String -Path 'demo/src/registry.ts' -Pattern \"id: 'webgl_interactive_buffergeometry'\" -Quiet)) { $ok = $false }; if ($ok) { exit 0 } else { exit 1 }"</automated>
  </verify>
  <acceptance_criteria>
    1. `demo/src/WebGLInteractiveBufferGeometry.tsx` exists with jsx pragma header.
    2. File uses `Raycaster` and a pointermove listener (verify by grep).
    3. File contains zero `as any` occurrences.
    4. Both screenshots exist and are non-empty.
    5. Claude APPROVED in conversation.
    6. Registry contains exactly one `id: 'webgl_interactive_buffergeometry'` under `interactive` category.
    7. `cd demo && pnpm build` exits 0.
  </acceptance_criteria>
  <done>WebGLInteractiveBufferGeometry ported 1:1 with Raycaster picking, screenshots captured, Claude approved, registered under `interactive`.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Port webgl_interactive_cubes_ortho</name>
  <files>
    demo/src/WebGLInteractiveCubesOrtho.tsx
    playwright.test/screenshots/ported/webgl_interactive_cubes_ortho.png
    playwright.test/screenshots/ref/webgl_interactive_cubes_ortho.png
  </files>
  <read_first>
    - demo/src/WebGLCamera.tsx (init3D scaffold)
    - .planning/phases/15-update-actual-implemented-tested-visually-checked-with-plann/15-PATTERNS.md
  </read_first>
  <action>
    1. Fetch `https://threejs.org/examples/webgl_interactive_cubes_ortho.html`. This demo renders many small cubes through an `OrthographicCamera` and uses Raycaster to highlight the one under the cursor. Reproduce 1:1: OrthographicCamera setup with frustum bounds derived from `aspect`, ambient + directional lights, instanced or individual cubes grid, raycaster, mouse highlight (e.g. color change or scale).

    2. Create `demo/src/WebGLInteractiveCubesOrtho.tsx` using init3D. OrthographicCamera resize handler must update `left/right/top/bottom` (not aspect ratio) and call `updateProjectionMatrix`.

    3. Type-check: `cd demo && pnpm tsc --noEmit`.

    4. dv visual verification:
       - `dv navigate --url "http://localhost:5173/#webgl_interactive_cubes_ortho" --port 9223`
       - Wait 3 seconds
       - `dv eval --js "!!document.querySelector('canvas')" --port 9223` returns `true`
       - `dv screenshot --output "playwright.test/screenshots/ported/webgl_interactive_cubes_ortho.png" --port 9223`
       - `dv navigate --url "https://threejs.org/examples/webgl_interactive_cubes_ortho.html" --port 9223`
       - Wait 3 seconds
       - `dv screenshot --output "playwright.test/screenshots/ref/webgl_interactive_cubes_ortho.png" --port 9223`

    5. Send to Claude: `Visual verify: webgl_interactive_cubes_ortho — ported (img1) vs reference (img2). Approve or reject?` Wait for APPROVE.

    6. After approval, register in the `interactive` CategoryEntry:
       `{ id: 'webgl_interactive_cubes_ortho', name: 'Interactive Cubes Ortho', category: 'interactive', component: () => import('./WebGLInteractiveCubesOrtho') },`
  </action>
  <verify>
    <automated>powershell -Command "$ok = $true; if (-not (Test-Path 'demo/src/WebGLInteractiveCubesOrtho.tsx')) { $ok = $false }; if (-not (Test-Path 'playwright.test/screenshots/ported/webgl_interactive_cubes_ortho.png')) { $ok = $false }; if (-not (Test-Path 'playwright.test/screenshots/ref/webgl_interactive_cubes_ortho.png')) { $ok = $false }; if (-not (Select-String -Path 'demo/src/WebGLInteractiveCubesOrtho.tsx' -Pattern '/\*\* @jsxImportSource woby \*/' -Quiet)) { $ok = $false }; if (-not (Select-String -Path 'demo/src/WebGLInteractiveCubesOrtho.tsx' -Pattern 'OrthographicCamera' -Quiet)) { $ok = $false }; if (Select-String -Path 'demo/src/WebGLInteractiveCubesOrtho.tsx' -Pattern 'as any' -Quiet) { $ok = $false }; if (-not (Select-String -Path 'demo/src/registry.ts' -Pattern \"id: 'webgl_interactive_cubes_ortho'\" -Quiet)) { $ok = $false }; if ($ok) { exit 0 } else { exit 1 }"</automated>
  </verify>
  <acceptance_criteria>
    1. `demo/src/WebGLInteractiveCubesOrtho.tsx` exists with jsx pragma header.
    2. File uses `OrthographicCamera` (verify by grep).
    3. File contains zero `as any` occurrences.
    4. Both screenshots exist and are non-empty.
    5. Claude APPROVED in conversation.
    6. Registry contains exactly one `id: 'webgl_interactive_cubes_ortho'` under `interactive` category.
    7. `cd demo && pnpm build` exits 0.
  </acceptance_criteria>
  <done>WebGLInteractiveCubesOrtho ported 1:1, screenshots captured, Claude approved, registered under `interactive`.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Port webgl_geometry_text_stroke</name>
  <files>
    demo/src/WebGLGeometryTextStroke.tsx
    playwright.test/screenshots/ported/webgl_geometry_text_stroke.png
    playwright.test/screenshots/ref/webgl_geometry_text_stroke.png
  </files>
  <read_first>
    - demo/src/WebGLCamera.tsx (init3D scaffold)
    - .planning/phases/15-update-actual-implemented-tested-visually-checked-with-plann/15-PATTERNS.md
    - demo/public/fonts/ (verify available font typeface files)
  </read_first>
  <action>
    1. Fetch `https://threejs.org/examples/webgl_geometry_text_stroke.html`. This demo renders 3D text using `TextGeometry` with a stroked outline appearance. Reproduce 1:1: FontLoader to load `fonts/helvetiker_regular.typeface.json` (or whichever the source uses — verify), TextGeometry with the loaded font, line-only material (`LineBasicMaterial` on EdgesGeometry or wireframe) for the stroke effect, lights, animate rotation.

    2. Verify the required font file exists under `demo/public/fonts/`. If missing, list available fonts in that directory and use the closest match, documenting the substitution in the SUMMARY (or copy the font from the Three.js examples public fonts directory).

    3. Create `demo/src/WebGLGeometryTextStroke.tsx` using init3D. Async font load means rendering only starts after the font resolves — set up the scene first, but only add the TextGeometry mesh inside the FontLoader callback.

    4. Type-check: `cd demo && pnpm tsc --noEmit`.

    5. **CRITICAL — registry.ts contention check before final write:**
       BEFORE adding to registry.ts, re-read the current state of `demo/src/registry.ts` to incorporate any parallel changes from Plan 02 Task 4 (which also adds a geometries entry). The registry edit MUST preserve Plan 02's `webgl_geometry_spline_editor` entry if Plan 02 has already registered it.

    6. dv visual verification:
       - `dv navigate --url "http://localhost:5173/#webgl_geometry_text_stroke" --port 9223`
       - Wait 4 seconds (font load)
       - `dv eval --js "!!document.querySelector('canvas')" --port 9223` returns `true`
       - `dv screenshot --output "playwright.test/screenshots/ported/webgl_geometry_text_stroke.png" --port 9223`
       - `dv navigate --url "https://threejs.org/examples/webgl_geometry_text_stroke.html" --port 9223`
       - Wait 4 seconds
       - `dv screenshot --output "playwright.test/screenshots/ref/webgl_geometry_text_stroke.png" --port 9223`

    7. Send to Claude: `Visual verify: webgl_geometry_text_stroke — ported (img1) vs reference (img2). Approve or reject?` Wait for APPROVE.

    8. After approval, register in the `geometries` CategoryEntry:
       `{ id: 'webgl_geometry_text_stroke', name: 'Text Stroke', category: 'geometries', component: () => import('./WebGLGeometryTextStroke') },`
  </action>
  <verify>
    <automated>powershell -Command "$ok = $true; if (-not (Test-Path 'demo/src/WebGLGeometryTextStroke.tsx')) { $ok = $false }; if (-not (Test-Path 'playwright.test/screenshots/ported/webgl_geometry_text_stroke.png')) { $ok = $false }; if (-not (Test-Path 'playwright.test/screenshots/ref/webgl_geometry_text_stroke.png')) { $ok = $false }; if (-not (Select-String -Path 'demo/src/WebGLGeometryTextStroke.tsx' -Pattern '/\*\* @jsxImportSource woby \*/' -Quiet)) { $ok = $false }; if (-not (Select-String -Path 'demo/src/WebGLGeometryTextStroke.tsx' -Pattern 'FontLoader' -Quiet)) { $ok = $false }; if (Select-String -Path 'demo/src/WebGLGeometryTextStroke.tsx' -Pattern 'as any' -Quiet) { $ok = $false }; if (-not (Select-String -Path 'demo/src/registry.ts' -Pattern \"id: 'webgl_geometry_text_stroke'\" -Quiet)) { $ok = $false }; if ($ok) { exit 0 } else { exit 1 }"</automated>
  </verify>
  <acceptance_criteria>
    1. `demo/src/WebGLGeometryTextStroke.tsx` exists with jsx pragma header.
    2. File uses `FontLoader` and `TextGeometry`.
    3. File contains zero `as any` occurrences.
    4. Both screenshots exist and are non-empty.
    5. Claude APPROVED in conversation.
    6. Registry contains exactly one `id: 'webgl_geometry_text_stroke'` under `geometries` category.
    7. If Plan 02 also added `webgl_geometry_spline_editor`, that entry is preserved (verified by grep).
    8. `cd demo && pnpm build` exits 0.
  </acceptance_criteria>
  <done>WebGLGeometryTextStroke ported 1:1, screenshots captured, Claude approved, registered under `geometries` without clobbering Plan 02's geometries addition.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 5: Port webgl_modifier_curve</name>
  <files>
    demo/src/WebGLModifierCurve.tsx
    playwright.test/screenshots/ported/webgl_modifier_curve.png
    playwright.test/screenshots/ref/webgl_modifier_curve.png
  </files>
  <read_first>
    - demo/src/WebGLCamera.tsx (init3D scaffold)
    - .planning/phases/15-update-actual-implemented-tested-visually-checked-with-plann/15-PATTERNS.md
  </read_first>
  <action>
    1. Fetch `https://threejs.org/examples/webgl_modifier_curve.html`. This demo uses the `CurveModifier` jsm/modifier to bend geometry along a curve. Reproduce 1:1: import `Flow` from `three/examples/jsm/modifiers/CurveModifier.js`, define a CatmullRomCurve3, create a mesh (typically text or a box), wrap with `Flow`, animate `flow.moveAlongCurve(delta)`.

    2. Verify the import path exists in installed three version: `three/examples/jsm/modifiers/CurveModifier.js`. If the file path is `Modifier` (older) vs `modifier` (newer), use whichever resolves — check `node_modules/three/examples/jsm/modifiers/` or test compile.

    3. Create `demo/src/WebGLModifierCurve.tsx` using init3D. Cleanup must dispose Flow's internal resources (the original Three.js example uses `flow` from CurveModifier — no explicit dispose API; relying on `renderer.dispose()` + geometry GC is acceptable). Standard cleanup contract still applies (rAF, listeners, GUI, renderer).

    4. Check `demo/src/registry.ts` for a `modifiers` CategoryEntry. If absent, add a new CategoryEntry: `{ id: 'modifiers', name: 'Modifiers', demos: [...] }` near the other webgl-feature categories (e.g. after `geometries`).

    5. Type-check: `cd demo && pnpm tsc --noEmit`.

    6. dv visual verification:
       - `dv navigate --url "http://localhost:5173/#webgl_modifier_curve" --port 9223`
       - Wait 4 seconds (may load font for text geometry)
       - `dv eval --js "!!document.querySelector('canvas')" --port 9223` returns `true`
       - `dv screenshot --output "playwright.test/screenshots/ported/webgl_modifier_curve.png" --port 9223`
       - `dv navigate --url "https://threejs.org/examples/webgl_modifier_curve.html" --port 9223`
       - Wait 4 seconds
       - `dv screenshot --output "playwright.test/screenshots/ref/webgl_modifier_curve.png" --port 9223`

    7. Send to Claude: `Visual verify: webgl_modifier_curve — ported (img1) vs reference (img2). Approve or reject?` Wait for APPROVE.

    8. After approval, register in the `modifiers` CategoryEntry:
       `{ id: 'webgl_modifier_curve', name: 'Curve Modifier', category: 'modifiers', component: () => import('./WebGLModifierCurve') },`

    9. End-of-plan cleanup: run `dv stop --port 9223` if Plan 02 has not already stopped it. Coordinate with Plan 02 — only the last-finishing plan should stop dv.
  </action>
  <verify>
    <automated>powershell -Command "$ok = $true; if (-not (Test-Path 'demo/src/WebGLModifierCurve.tsx')) { $ok = $false }; if (-not (Test-Path 'playwright.test/screenshots/ported/webgl_modifier_curve.png')) { $ok = $false }; if (-not (Test-Path 'playwright.test/screenshots/ref/webgl_modifier_curve.png')) { $ok = $false }; if (-not (Select-String -Path 'demo/src/WebGLModifierCurve.tsx' -Pattern '/\*\* @jsxImportSource woby \*/' -Quiet)) { $ok = $false }; if (-not (Select-String -Path 'demo/src/WebGLModifierCurve.tsx' -Pattern 'CurveModifier' -Quiet)) { $ok = $false }; if (Select-String -Path 'demo/src/WebGLModifierCurve.tsx' -Pattern 'as any' -Quiet) { $ok = $false }; if (-not (Select-String -Path 'demo/src/registry.ts' -Pattern \"id: 'webgl_modifier_curve'\" -Quiet)) { $ok = $false }; if ($ok) { exit 0 } else { exit 1 }"</automated>
  </verify>
  <acceptance_criteria>
    1. `demo/src/WebGLModifierCurve.tsx` exists with jsx pragma header.
    2. File imports from `three/examples/jsm/modifiers/CurveModifier.js` (or whichever path resolves).
    3. File contains zero `as any` occurrences.
    4. Both screenshots exist and are non-empty.
    5. Claude APPROVED in conversation.
    6. Registry contains exactly one `id: 'webgl_modifier_curve'`, either under a pre-existing `modifiers` category OR under a newly-added `modifiers` CategoryEntry.
    7. `cd demo && pnpm build` exits 0 after all 5 tasks.
  </acceptance_criteria>
  <done>WebGLModifierCurve ports CurveModifier example 1:1, screenshots captured, Claude approved, registered under `modifiers`, build passes; dv stopped if last plan to finish.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dev server → browser | dv-controlled Chrome navigates to localhost; localhost is trusted |
| browser → threejs.org | reference screenshots; external read-only; HTTPS-validated |
| executor → claude conversation | image @-attachments for approval; image bytes not executed |
| Plan 02 ⇄ Plan 03 | shared file `demo/src/registry.ts` — parallel edit hazard |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-03-01 | Tampering | dv screenshot writes to playwright.test/screenshots | accept | PNG output is inert; explicit per-task paths |
| T-15-03-02 | Spoofing | threejs.org reference page | accept | HTTPS; visual comparison is human-judged |
| T-15-03-03 | DoS | Chrome process from `dv start` | mitigate | profile-qmdj-2 isolates from orchestrator workers; `dv stop` at plan end |
| T-15-03-04 | Tampering | demo/src/registry.ts concurrent edits with Plan 02 | mitigate | Task 4 explicitly re-reads registry.ts before write; each task asserts exactly-one occurrence via grep; build must pass after each task |
| T-15-03-05 | Information disclosure | screenshot may capture other tab content | accept | dv operates on single tab; navigation precedes screenshot |
| T-15-03-06 | Tampering | new font/asset files in demo/public/ | mitigate | only copy from known Three.js examples source; do not download from untrusted URLs |
</threat_model>

<verification>
End-of-plan checks (after Task 5):
1. `cd demo && pnpm build` exits 0.
2. `(Get-ChildItem demo/src -Filter *.tsx | Where-Object { $_.Name -notmatch '^(index|menu-frame|canvas-frame|AutoSizeCanvas|Sector|Box3ce)' }).Count` increased by exactly 5 vs the Plan 02 endpoint baseline.
3. Registry contains 5 new entries: `webgl_camera_logarithmicdepthbuffer` (or `_canonical` variant), `webgl_interactive_buffergeometry`, `webgl_interactive_cubes_ortho`, `webgl_geometry_text_stroke`, `webgl_modifier_curve`.
4. All 10 screenshot files exist in `playwright.test/screenshots/ported/` and `playwright.test/screenshots/ref/`.
5. No `as any` introduced in any new TSX file.
6. If Plan 02 also completed: combined registry has +10 unique IDs vs Plan 01 endpoint; no clobbered entries (`webgl_animation_walk`, `webgl_animation_locomotive`, `webgl_clipping_stencil`, `webgl_geometry_spline_editor`, and the hemisphere id all still present).
7. `dv stop --port 9223` ran successfully (coordinated with Plan 02 if running in parallel).
</verification>

<success_criteria>
- 5 ported TSX files exist, each starting with `/** @jsxImportSource woby */` and using init3D pattern
- 10 screenshot pairs captured via dv
- Claude APPROVED all 5 visual comparisons in the conversation
- 5 new registry entries added under correct categories (cameras, interactive×2, geometries, modifiers)
- New `interactive` or `modifiers` CategoryEntry created if missing (preserves all existing categories)
- Zero `as any` in any new file (NFR-2.1)
- `cd demo && pnpm build` exits 0
- Plan 02's parallel additions to `geometries` preserved (no clobber)
</success_criteria>

<output>
Create `.planning/phases/15-update-actual-implemented-tested-visually-checked-with-plann/15-03-SUMMARY.md` when done — record:
- Each demo's id, file name, category
- Claude's approval message excerpt per demo
- Any rework cycles
- Whether new `interactive` and/or `modifiers` categories were created
- Final registry count delta (200 → 205 if Plan 02 incomplete; 210 if both complete)
- Build output confirmation
- Coordination note on Plan 02 if registry merge required re-read
</output>
