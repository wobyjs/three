---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - code/examples/webgl/_template.tsx
  - .planning/phase-1/PATTERNS.md
  - code/examples/webgl/animation/
  - code/examples/webgl/camera/
  - code/examples/webgl/geometries/
  - code/examples/webgl/lights/
  - code/examples/webgl/loaders/
  - code/examples/webgl/interactive/
  - code/examples/webgl/advanced/
  - code/examples/webgl/materials/
autonomous: true
requirements:
  - FR-1.1
  - FR-2.1
  - FR-2.2
  - FR-2.4
  - FR-3.1
must_haves:
  truths:
    - "Template file exists with standard port structure"
    - "PATTERNS.md documents all porting patterns discovered"
    - "Directory structure ready for example categories"
  artifacts:
    - path: "code/examples/webgl/_template.tsx"
      provides: "Reusable scaffold for porting examples"
      min_lines: 40
      contains: "@jsxImportSource @woby/three"
    - path: ".planning/phase-1/PATTERNS.md"
      provides: "Documentation of porting patterns"
      min_lines: 50
      contains: "## Pattern"
  key_links:
    - from: "code/examples/webgl/_template.tsx"
      to: "@woby/three JSX runtime"
      via: "@jsxImportSource directive"
      pattern: "@jsxImportSource @woby/three"
---

<objective>
Create foundation infrastructure for porting Three.js examples: directory structure, reusable template scaffold, and pattern documentation.

Purpose: Establish consistent patterns and tooling for all subsequent porting work. The template provides a starting point for each example, while PATTERNS.md captures the conventions discovered during research.

Output: Directory structure, template file, and pattern documentation.
</objective>

<execution_context>
@$HOME/.claude-glm-glm/get-shit-done/workflows/execute-plan.md
@$HOME/.claude-glm-glm/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phase-1/01-RESEARCH.md

<interfaces>
<!-- Key patterns from existing codebase -->

From demo/src/WebGLGeometries.tsx:
```tsx
/** @jsxImportSource @woby/three */
import { $, $$ } from "woby"
import { Canvas3D } from '@woby/three/lib/components/Canvas3D'
import { OrbitControls } from '@woby/three/examples/jsm/controls/OrbitControls'
```

From test/jsx.test.tsx:
```tsx
// @jsx _h
// @jsxFrag _Fragment
import './setup'
import './registry-seeds'
// Pattern: Setup first, registry seeds second, wrapper imports third
const makeCtx = () => ({ frames: [], scenes: [], cameras: [], renderers: [], update: $(0) })
```

From test/geometries.test.ts:
```tsx
function checkRegistration(name: string, threeKey: string, consKey = name, objKey = name, defKey = name) {
    test(`${name} registered in consParams`, () => ok((consParams as any)[consKey] !== undefined))
    test(`${name} registered in objProps`, () => ok((objProps as any)[objKey] !== undefined))
    test(`${name} registered in defaults`, () => ok((defaults as any)[defKey] !== undefined))
    test(`Three.${threeKey} registered`, () => ok((Three as any)[threeKey] !== undefined))
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create directory structure for webgl examples</name>
  <files>
    code/examples/webgl/animation/
    code/examples/webgl/camera/
    code/examples/webgl/geometries/
    code/examples/webgl/lights/
    code/examples/webgl/loaders/
    code/examples/webgl/interactive/
    code/examples/webgl/advanced/
    code/examples/webgl/materials/
  </files>
  <action>
    Create the directory structure for organizing ported webgl examples by category.

    Directories to create:
    - code/examples/webgl/animation/ - Animation examples (keyframes, multiple)
    - code/examples/webgl/camera/ - Camera examples
    - code/examples/webgl/geometries/ - Geometry showcase examples
    - code/examples/webgl/lights/ - Lighting examples (spotlight, hemisphere)
    - code/examples/webgl/loaders/ - Loader examples (GLTF)
    - code/examples/webgl/interactive/ - Interactive/raycasting examples
    - code/examples/webgl/advanced/ - Advanced rendering examples (SimpleGI)
    - code/examples/webgl/materials/ - Material examples (variations)

    Use Node.js fs.mkdirSync with recursive option or PowerShell New-Item -ItemType Directory -Force.
  </action>
  <verify>
    <automated>ls code/examples/webgl/</automated>
  </verify>
  <done>
    All 8 directories exist under code/examples/webgl/
  </done>
</task>

<task type="auto">
  <name>Task 2: Create example port template scaffold</name>
  <files>code/examples/webgl/_template.tsx</files>
  <action>
    Create a reusable template file that serves as the starting point for porting Three.js examples.

    The template MUST include:
    1. @jsxImportSource @woby/three directive (first line)
    2. Standard imports from woby and @woby/three
    3. Component structure with Canvas3D wrapper
    4. Scene setup: perspectiveCamera, scene, ambientLight, directionalLight
    5. useFrame hook example for animation
    6. OrbitControls for camera interaction
    7. Comments explaining each section

    Template structure:
    ```tsx
    /** @jsxImportSource @woby/three */

    import { $, $$, useEffect } from "woby"
    import { useFrame, useThree, useRenderer, useCamera } from '@woby/three'
    import * as THREE from 'three'

    /**
     * Template for porting Three.js examples to @woby/three JSX syntax.
     *
     * Porting checklist:
     * 1. Replace THREE.* constructor calls with JSX elements
     * 2. Use args prop for constructor parameters
     * 3. Use kebab-case for nested properties (shadow-camera-far)
     * 4. Use $$() for context arrays (never $())
     * 5. Add useFrame for animation loops
     * 6. Import required wrapper files for class registration
     */
    export const TemplateExample = () => {
        // Reactive state
        const ref = $<THREE.Mesh>()
        const { scene } = useThree()

        // Animation frame callback
        useFrame(() => {
            const mesh = $$(ref)
            if (mesh) {
                mesh.rotation.x += 0.01
                mesh.rotation.y += 0.01
            }
        })

        return (
            <canvas3D>
                <perspectiveCamera position={[0, 0, 5]} />
                <scene>
                    <ambientLight intensity={0.5} />
                    <directionalLight position={[5, 10, 7]} intensity={1} />
                    <mesh ref={ref}>
                        <boxGeometry args={[1, 1, 1]} />
                        <meshStandardMaterial color="orange" />
                    </mesh>
                </scene>
                <orbitControls />
            </canvas3D>
        )
    }
    ```

    CRITICAL: The template must demonstrate:
    - args prop for constructor params (FR-2.1)
    - Reactive refs with $() and $$() (FR-2.4)
    - useFrame for animation (FR-4.1)
    - Proper JSX nesting for scene graph (FR-3.1)
  </action>
  <verify>
    <automated>tsc --noEmit code/examples/webgl/_template.tsx 2>&1 | head -20</automated>
  </verify>
  <done>
    _template.tsx exists with @jsxImportSource directive, standard imports, Canvas3D structure, useFrame example, and porting checklist comments
  </done>
</task>

<task type="auto">
  <name>Task 3: Create PATTERNS.md documentation</name>
  <files>.planning/phase-1/PATTERNS.md</files>
  <action>
    Create documentation file capturing all porting patterns discovered during research and implementation.

    The PATTERNS.md file MUST document:
    1. Core JSX patterns (args prop, kebab-case, reactive props)
    2. Scene graph assembly patterns
    3. Animation and render loop patterns
    4. Context access patterns ($$ vs $)
    5. Common pitfalls and how to avoid them
    6. Import order requirements
    7. Testing patterns

    Structure:
    ```markdown
    # Phase 1: Porting Patterns

    ## Pattern 1: Constructor Args via args Prop
    ...

    ## Pattern 2: Nested Properties via Kebab-Case
    ...

    ## Pattern 3: Reactive Props with Observables
    ...

    ## Pattern 4: Scene Graph Assembly
    ...

    ## Pattern 5: Animation with useFrame
    ...

    ## Anti-Patterns to Avoid
    ...

    ## Import Order Requirements
    ...
    ```

    Reference the patterns documented in RESEARCH.md and add any new patterns discovered during implementation.
  </action>
  <verify>
    <automated>grep -c "## Pattern" .planning/phase-1/PATTERNS.md</automated>
  </verify>
  <done>
    PATTERNS.md exists with at least 5 documented patterns, anti-patterns section, and import order requirements
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | This plan creates infrastructure files only, no external data processing |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | N/A | Infrastructure files | accept | No security-relevant operations in this plan |
</threat_model>

<verification>
## Phase Verification Gates

1. **Directory Structure**: All 8 category directories exist
   - Command: `ls code/examples/webgl/`

2. **Template Syntax**: Template compiles without TypeScript errors
   - Command: `tsc --noEmit code/examples/webgl/_template.tsx`

3. **Template Directive**: @jsxImportSource directive present
   - Command: `grep "@jsxImportSource @woby/three" code/examples/webgl/_template.tsx`

4. **Patterns Documentation**: At least 5 patterns documented
   - Command: `grep -c "## Pattern" .planning/phase-1/PATTERNS.md`
</verification>

<success_criteria>
- [ ] 8 category directories exist under code/examples/webgl/
- [ ] _template.tsx exists with valid JSX syntax
- [ ] @jsxImportSource @woby/three directive present in template
- [ ] PATTERNS.md exists with at least 5 documented patterns
- [ ] Template demonstrates args prop, reactive refs, useFrame, and JSX nesting
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>
