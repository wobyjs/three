---
phase: 01-foundation
plan: 02
type: execute
wave: 2
depends_on: [01-01]
files_modified:
  - code/examples/webgl/geometries/Geometries.tsx
  - code/examples/webgl/camera/Camera.tsx
  - code/examples/webgl/lights/Hemisphere.tsx
  - code/examples/webgl/materials/Variations.tsx
autonomous: true
requirements:
  - FR-1.1
  - FR-2.1
  - FR-2.2
  - FR-2.4
  - FR-3.1
  - FR-5.1
must_haves:
  truths:
    - "4 core examples exist as JSX components"
    - "Each example follows established porting patterns"
    - "Examples compile without TypeScript errors"
  artifacts:
    - path: "code/examples/webgl/geometries/Geometries.tsx"
      provides: "webgl_geometries port"
      min_lines: 60
      contains: "@jsxImportSource @woby/three"
    - path: "code/examples/webgl/camera/Camera.tsx"
      provides: "webgl_camera port"
      min_lines: 80
      contains: "@jsxImportSource @woby/three"
    - path: "code/examples/webgl/lights/Hemisphere.tsx"
      provides: "webgl_lights_hemisphere port"
      min_lines: 40
      contains: "@jsxImportSource @woby/three"
    - path: "code/examples/webgl/materials/Variations.tsx"
      provides: "webgl_materials_variations port"
      min_lines: 50
      contains: "@jsxImportSource @woby/three"
  key_links:
    - from: "code/examples/webgl/**/*.tsx"
      to: "@woby/three JSX runtime"
      via: "@jsxImportSource directive"
      pattern: "@jsxImportSource @woby/three"
---

<objective>
Port 4 core WebGL examples from Three.js vanilla JS to @woby/three JSX syntax: geometries, camera, hemisphere light, and materials variations.

Purpose: These are lower-complexity examples that establish the porting patterns for common Three.js features. They serve as reference implementations for geometries, camera systems, lighting, and materials.

Output: 4 JSX example files following established patterns.
</objective>

<execution_context>
@$HOME/.claude-glm-glm/get-shit-done/workflows/execute-plan.md
@$HOME/.claude-glm-glm/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phase-1/01-RESEARCH.md
@.planning/phase-1/PATTERNS.md

<interfaces>
<!-- Key patterns from template and existing code -->

From code/examples/webgl/_template.tsx:
```tsx
/** @jsxImportSource @woby/three */
import { $, $$, useEffect } from "woby"
import { useFrame, useThree, useRenderer, useCamera } from '@woby/three'
import * as THREE from 'three'
```

From RESEARCH.md Pattern 2:
```tsx
// Constructor args via args prop
<boxGeometry args={[1, 1, 1]} />
<pointLight args={[0xff0000, 1, 100]} />
```

From RESEARCH.md Pattern 3:
```tsx
// Nested properties via kebab-case
<pointLight shadow-camera-far={333} />
```

From RESEARCH.md Anti-Patterns:
- Use $$() for context arrays, never $()
- No as any casts - investigate type issues
- Always provide both geometry and material children in mesh
- Reset viewport after inset rendering
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Port webgl_geometries example</name>
  <files>code/examples/webgl/geometries/Geometries.tsx</files>
  <action>
    Port webgl_geometries from Three.js examples to JSX syntax.

    Source reference: https://threejs.org/examples/webgl_geometries.html

    Key Three.js classes used:
    - Multiple geometry types: SphereGeometry, IcosahedronGeometry, OctahedronGeometry, TetrahedronGeometry, PlaneGeometry, BoxGeometry, CircleGeometry, RingGeometry, CylinderGeometry, LatheGeometry, TorusGeometry, TorusKnotGeometry, CapsuleGeometry
    - THREE.MeshPhongMaterial, THREE.TextureLoader

    Implementation requirements:
    1. Start with @jsxImportSource @woby/three directive
    2. Import all required geometry wrappers
    3. Create 20 mesh objects in 4x5 grid layout
    4. Use OrbitControls with autoRotate for camera orbit
    5. Apply MeshPhongMaterial with texture

    Critical considerations:
    - 20 mesh objects in 4x5 grid layout
    - Camera orbit animation (via OrbitControls autoRotate)
    - Multiple geometry types (14+)
    - Texture loading for materials
    - ParametricGeometry from addons (if used)

    Reference the existing demo/src/WebGLGeometries.tsx for patterns.
  </action>
  <verify>
    <automated>tsc --noEmit code/examples/webgl/geometries/Geometries.tsx 2>&1 | head -20</automated>
  </verify>
  <done>
    Geometries.tsx exists with @jsxImportSource directive, all 14+ geometry types, 4x5 grid layout, auto-rotating camera
  </done>
</task>

<task type="auto">
  <name>Task 2: Port webgl_camera example</name>
  <files>code/examples/webgl/camera/Camera.tsx</files>
  <action>
    Port webgl_camera from Three.js examples to JSX syntax.

    Source reference: https://threejs.org/examples/webgl_camera.html

    Key Three.js classes used:
    - THREE.PerspectiveCamera, THREE.OrthographicCamera
    - THREE.CameraHelper, THREE.Group
    - THREE.Points, THREE.PointsMaterial
    - THREE.BufferGeometry, THREE.Float32BufferAttribute

    Implementation requirements:
    1. Start with @jsxImportSource @woby/three directive
    2. Implement camera switching via keyboard (O/P keys)
    3. Create split-screen rendering with viewports
    4. Add CameraHelper for visualization
    5. Create particle system with 10,000 points

    Critical considerations:
    - Split-screen rendering (left/right viewports) - use setViewport/setScissor
    - Camera switching via keyboard (O/P keys)
    - CameraHelper for visualization
    - Particle system with 10,000 points
    - CRITICAL: Reset viewport after inset rendering (per memory feedback)

    Use useEffect for keyboard event handling.
  </action>
  <verify>
    <automated>tsc --noEmit code/examples/webgl/camera/Camera.tsx 2>&1 | head -20</automated>
  </verify>
  <done>
    Camera.tsx exists with @jsxImportSource directive, camera switching, split-screen rendering, viewport reset
  </done>
</task>

<task type="auto">
  <name>Task 3: Port webgl_lights_hemisphere example</name>
  <files>code/examples/webgl/lights/Hemisphere.tsx</files>
  <action>
    Port webgl_lights_hemisphere from Three.js examples to JSX syntax.

    Source reference: https://threejs.org/examples/webgl_lights_hemisphere.html

    Key Three.js classes used:
    - THREE.HemisphereLight
    - THREE.MeshStandardMaterial
    - Basic geometry (sphere or similar)

    Implementation requirements:
    1. Start with @jsxImportSource @woby/three directive
    2. Create HemisphereLight with sky/ground colors
    3. Demonstrate outdoor lighting effect
    4. Add OrbitControls for camera interaction

    This is a simpler example demonstrating hemisphere light for outdoor scenes.
  </action>
  <verify>
    <automated>tsc --noEmit code/examples/webgl/lights/Hemisphere.tsx 2>&1 | head -20</automated>
  </verify>
  <done>
    Hemisphere.tsx exists with @jsxImportSource directive, HemisphereLight setup, outdoor lighting demonstration
  </done>
</task>

<task type="auto">
  <name>Task 4: Port webgl_materials_variations example</name>
  <files>code/examples/webgl/materials/Variations.tsx</files>
  <action>
    Port webgl_materials_variations from Three.js examples to JSX syntax.

    Source reference: https://threejs.org/examples/webgl_materials_variations.html

    Key Three.js classes used:
    - THREE.MeshStandardMaterial
    - THREE.MeshPhysicalMaterial
    - Various geometry types for demonstration

    Implementation requirements:
    1. Start with @jsxImportSource @woby/three directive
    2. Create side-by-side comparison of material settings
    3. Demonstrate roughness, metalness variations
    4. Use MeshStandardMaterial and/or MeshPhysicalMaterial

    Critical considerations:
    - Material property variations (roughness, metalness, etc.)
    - Side-by-side comparison layout
    - Consistent lighting for fair comparison
  </action>
  <verify>
    <automated>tsc --noEmit code/examples/webgl/materials/Variations.tsx 2>&1 | head -20</automated>
  </verify>
  <done>
    Variations.tsx exists with @jsxImportSource directive, material variations demonstration, side-by-side layout
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User input | Keyboard events in camera example - untrusted input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-02 | Tampering | Keyboard events | accept | No sensitive operations triggered by keyboard - low risk |
</threat_model>

<verification>
## Phase Verification Gates

1. **Syntax Verification**: All 4 example files compile without TypeScript errors
   - Command: `tsc --noEmit code/examples/webgl/geometries/Geometries.tsx code/examples/webgl/camera/Camera.tsx code/examples/webgl/lights/Hemisphere.tsx code/examples/webgl/materials/Variations.tsx`

2. **Directive Verification**: @jsxImportSource directive present in all files
   - Command: `grep -l "@jsxImportSource @woby/three" code/examples/webgl/geometries/Geometries.tsx code/examples/webgl/camera/Camera.tsx code/examples/webgl/lights/Hemisphere.tsx code/examples/webgl/materials/Variations.tsx | wc -l` (expect 4)

3. **Pattern Compliance**: Each example follows established patterns
   - Canvas3D wrapper used
   - scene/renderer/camera as children
   - useFrame for animation (where applicable)
</verification>

<success_criteria>
- [ ] 4 core example files exist in code/examples/webgl/
- [ ] Each example has @jsxImportSource @woby/three directive
- [ ] All examples compile without TypeScript errors
- [ ] No `as any` casts used
- [ ] `$$` used for context array access (not `$`)
- [ ] Viewport reset after custom rendering (Camera.tsx)
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-02-SUMMARY.md`
</output>
