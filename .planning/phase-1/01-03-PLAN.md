---
phase: 01-foundation
plan: 03
type: execute
wave: 3
depends_on: [01-01, 01-02]
files_modified:
  - code/examples/webgl/animation/Keyframes.tsx
  - code/examples/webgl/animation/Multiple.tsx
  - code/examples/webgl/lights/Spotlight.tsx
  - code/examples/webgl/loaders/GLTF.tsx
autonomous: true
requirements:
  - FR-1.1
  - FR-2.1
  - FR-2.2
  - FR-2.4
  - FR-3.1
  - FR-5.1
must_haves:
  truths:
    - "4 advanced examples exist as JSX components"
    - "Animation examples demonstrate keyframe and multiple clip playback"
    - "Loader example demonstrates GLTF loading pattern"
    - "Spotlight example demonstrates shadow mapping"
  artifacts:
    - path: "code/examples/webgl/animation/Keyframes.tsx"
      provides: "webgl_animation_keyframes port"
      min_lines: 50
      contains: "@jsxImportSource @woby/three"
    - path: "code/examples/webgl/animation/Multiple.tsx"
      provides: "webgl_animation_multiple port"
      min_lines: 50
      contains: "@jsxImportSource @woby/three"
    - path: "code/examples/webgl/lights/Spotlight.tsx"
      provides: "webgl_lights_spotlight port"
      min_lines: 60
      contains: "@jsxImportSource @woby/three"
    - path: "code/examples/webgl/loaders/GLTF.tsx"
      provides: "webgl_loader_gltf port"
      min_lines: 50
      contains: "@jsxImportSource @woby/three"
  key_links:
    - from: "code/examples/webgl/**/*.tsx"
      to: "@woby/three JSX runtime"
      via: "@jsxImportSource directive"
      pattern: "@jsxImportSource @woby/three"
    - from: "code/examples/webgl/loaders/GLTF.tsx"
      to: "three/examples/jsm/loaders"
      via: "GLTFLoader import"
      pattern: "GLTFLoader"
---

<objective>
Port 4 advanced WebGL examples from Three.js vanilla JS to @woby/three JSX syntax: keyframe animation, multiple animations, spotlight with shadows, and GLTF loader.

Purpose: These examples demonstrate more complex Three.js features including animation systems, shadow mapping, and external model loading. They serve as reference implementations for advanced features.

Output: 4 JSX example files following established patterns.
</objective>

<execution_context>
@$HOME/.claude-glm-glm/get-shit-done/workflows/execute-plan.md
@$HOME/.claude-glm-glm/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phase-1/01-RESEARCH.md
@.planning/phase-1/PATTERNS.md

<interfaces>
<!-- Key patterns from template and prior examples -->

From code/examples/webgl/_template.tsx:
```tsx
/** @jsxImportSource @woby/three */
import { $, $$, useEffect } from "woby"
import { useFrame, useThree, useRenderer, useCamera } from '@woby/three'
import * as THREE from 'three'
```

From RESEARCH.md - Animation pattern:
```tsx
const mixer = $<THREE.AnimationMixer>()
const clock = new THREE.Clock()

useFrame(() => {
    const m = $$(mixer)
    if (m) m.update(clock.getDelta())
})
```

From RESEARCH.md - GLTFLoader pattern:
```tsx
import { GLTFLoader } from 'three/examples/jsm/loaders/GLTFLoader.js'

useEffect(() => {
    const loader = new GLTFLoader()
    loader.load('https://threejs.org/examples/models/gltf/...', (gltf) => {
        $$(scene)?.add(gltf.scene)
        if (gltf.animations.length > 0) {
            mixer(new THREE.AnimationMixer(gltf.scene))
            $$(mixer)?.clipAction(gltf.animations[0]).play()
        }
    })
})
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Port webgl_animation_keyframes example</name>
  <files>code/examples/webgl/animation/Keyframes.tsx</files>
  <action>
    Port webgl_animation_keyframes from Three.js examples to JSX syntax.

    Source reference: https://threejs.org/examples/webgl_animation_keyframes.html

    Key Three.js classes used:
    - THREE.Timer, THREE.WebGLRenderer, THREE.Scene
    - THREE.PMREMGenerator, THREE.PerspectiveCamera
    - THREE.AnimationMixer (from addons)
    - OrbitControls, DRACOLoader, GLTFLoader (from addons)

    Implementation requirements:
    1. Start with @jsxImportSource @woby/three directive
    2. Use THREE.Timer for consistent animation timing
    3. Implement AnimationMixer for keyframe playback
    4. Load GLTF model with embedded animations
    5. Use DRACOLoader for compressed models

    External model URL: https://threejs.org/examples/models/gltf/LittlestTokyo.glb

    Note: This is a MEDIUM-HIGH complexity example. Implement full functionality - no simplified version. If model loading requires specific setup, document in PATTERNS.md.
  </action>
  <verify>
    <automated>tsc --noEmit code/examples/webgl/animation/Keyframes.tsx 2>&1 | head -20</automated>
  </verify>
  <done>
    Keyframes.tsx exists with @jsxImportSource directive, AnimationMixer setup, GLTF loading, keyframe animation playback
  </done>
</task>

<task type="auto">
  <name>Task 2: Port webgl_animation_multiple example</name>
  <files>code/examples/webgl/animation/Multiple.tsx</files>
  <action>
    Port webgl_animation_multiple from Three.js examples to JSX syntax.

    Source reference: https://threejs.org/examples/webgl_animation_multiple.html

    Key Three.js classes used:
    - THREE.AnimationMixer
    - Multiple AnimationClip actions
    - GLTFLoader

    Implementation requirements:
    1. Start with @jsxImportSource @woby/three directive
    2. Create AnimationMixer for model
    3. Support multiple animation clips
    4. Implement play/pause/blend controls (can use reactive state)
    5. Load model with multiple animations

    This example demonstrates managing multiple animation clips on a single model.
  </action>
  <verify>
    <automated>tsc --noEmit code/examples/webgl/animation/Multiple.tsx 2>&1 | head -20</automated>
  </verify>
  <done>
    Multiple.tsx exists with @jsxImportSource directive, multiple animation clips, play/pause/blend controls
  </done>
</task>

<task type="auto">
  <name>Task 3: Port webgl_lights_spotlight example</name>
  <files>code/examples/webgl/lights/Spotlight.tsx</files>
  <action>
    Port webgl_lights_spotlight from Three.js examples to JSX syntax.

    Source reference: https://threejs.org/examples/webgl_lights_spotlight.html

    Key Three.js classes used:
    - THREE.SpotLight, THREE.SpotLightHelper, THREE.HemisphereLight
    - THREE.CameraHelper, THREE.PlaneGeometry
    - THREE.MeshLambertMaterial, THREE.TextureLoader

    Implementation requirements:
    1. Start with @jsxImportSource @woby/three directive
    2. Create SpotLight with shadow mapping enabled
    3. Implement orbiting light animation via useFrame
    4. Add SpotLightHelper for visualization
    5. Configure shadow properties (angle, penumbra, decay, distance)

    Critical considerations:
    - SpotLight configuration (angle, penumbra, decay, distance)
    - Shadow mapping enabled on renderer
    - Light helper visualization
    - Orbiting light animation

    Use kebab-case for nested shadow properties: shadow-camera-far, shadow-mapSize-width, etc.
  </action>
  <verify>
    <automated>tsc --noEmit code/examples/webgl/lights/Spotlight.tsx 2>&1 | head -20</automated>
  </verify>
  <done>
    Spotlight.tsx exists with @jsxImportSource directive, SpotLight with shadows, orbiting animation, helper visualization
  </done>
</task>

<task type="auto">
  <name>Task 4: Port webgl_loader_gltf example</name>
  <files>code/examples/webgl/loaders/GLTF.tsx</files>
  <action>
    Port webgl_loader_gltf from Three.js examples to JSX syntax.

    Source reference: https://threejs.org/examples/webgl_loader_gltf.html

    Key Three.js classes used:
    - THREE.GLTFLoader, THREE.DRACOLoader
    - THREE.AnimationMixer, THREE.Box3, THREE.Vector3
    - THREE.ACESFilmicToneMapping

    Implementation requirements:
    1. Start with @jsxImportSource @woby/three directive
    2. Import GLTFLoader from three/examples/jsm/loaders
    3. Implement model loading with useEffect
    4. Add animation playback via AnimationMixer
    5. Implement bounding box camera fitting
    6. Use ACESFilmicToneMapping for rendering

    Critical considerations:
    - GLTFLoader for model loading
    - DRACOLoader for compressed models (optional)
    - Animation playback via AnimationMixer
    - Bounding box camera fitting
    - HDR environment loading (implement fully, no simplification)

    Model URLs from threejs.org or document local hosting requirements in PATTERNS.md.
  </action>
  <verify>
    <automated>tsc --noEmit code/examples/webgl/loaders/GLTF.tsx 2>&1 | head -20</automated>
  </verify>
  <done>
    GLTF.tsx exists with @jsxImportSource directive, GLTFLoader integration, animation support, camera fitting
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| External model URLs | GLTF models loaded from threejs.org - untrusted content |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-03 | Tampering | GLTFLoader | mitigate | Validate model URLs against allowlist (threejs.org, local) |
| T-01-04 | Information Disclosure | External models | accept | Models are public examples - no sensitive data |
</threat_model>

<verification>
## Phase Verification Gates

1. **Syntax Verification**: All 4 example files compile without TypeScript errors
   - Command: `tsc --noEmit code/examples/webgl/animation/Keyframes.tsx code/examples/webgl/animation/Multiple.tsx code/examples/webgl/lights/Spotlight.tsx code/examples/webgl/loaders/GLTF.tsx`

2. **Directive Verification**: @jsxImportSource directive present in all files
   - Command: `grep -l "@jsxImportSource @woby/three" code/examples/webgl/animation/Keyframes.tsx code/examples/webgl/animation/Multiple.tsx code/examples/webgl/lights/Spotlight.tsx code/examples/webgl/loaders/GLTF.tsx | wc -l` (expect 4)

3. **Pattern Compliance**: Each example follows established patterns
   - Canvas3D wrapper used
   - useFrame for animation
   - Proper loader setup with useEffect
</verification>

<success_criteria>
- [ ] 4 advanced example files exist in code/examples/webgl/
- [ ] Each example has @jsxImportSource @woby/three directive
- [ ] All examples compile without TypeScript errors
- [ ] Animation examples use AnimationMixer correctly
- [ ] GLTF example uses GLTFLoader with proper setup
- [ ] Spotlight example has shadow mapping enabled
- [ ] No `as any` casts used
- [ ] `$$` used for context array access (not `$`)
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-03-SUMMARY.md`
</output>
